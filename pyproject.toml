[project]
name = "agentic-crm-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "khalidabdelrahman", email = "khalid3abdo@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "azure-ai-projects>=2.4.0",
    "azure-identity>=1.25.3",
    "mcp<2",
    "python-dotenv>=1.2.2",
]

[project.scripts]
agentic-crm-demo = "agentic_crm_demo:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
